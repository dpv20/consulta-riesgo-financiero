export function HomePage() {
  return (
    <main className="mx-auto flex min-h-full max-w-2xl flex-col justify-center gap-4 p-8">
      <h1 className="text-3xl font-semibold tracking-tight">Starter listo</h1>
      <p className="text-neutral-600">
        Vite + React + TypeScript + Tailwind + React Query + Zustand + Vitest.
      </p>
    </main>
  )
}
